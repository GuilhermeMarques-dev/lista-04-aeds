#include <stdio.h>

float media(int a, int b, int c) {
    return (a + b + c) / 3.0;
}

int main() {
    int x, y, z;
    scanf("%d %d %d", &x, &y, &z);
    printf("Media: %.2f\n", media(x, y, z));
    return 0;
}
